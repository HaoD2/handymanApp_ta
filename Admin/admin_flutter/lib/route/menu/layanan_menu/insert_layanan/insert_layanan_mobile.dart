import 'package:flutter/material.dart';

class insertLayananMobile extends StatelessWidget {
  const insertLayananMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
